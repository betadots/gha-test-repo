on:
  - push

name: Build 🛢️ container

env:
  DAY_OF_WEEK: Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday

jobs:
  build_job:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - run: echo ${{ fromJSON(env.DAY_OF_WEEK) }}
